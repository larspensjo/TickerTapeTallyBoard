[package]
name = "ticker-tape-tally-board-backend"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.8"
log = "0.4"
simplelog = "0.12"
tokio = { version = "1", features = ["macros", "net", "rt-multi-thread"] }
